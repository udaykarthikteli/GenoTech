[project]
name = "genotech"
version = "0.1.0"
description = "Flask-based bioinformatics web app for real-time DNA sequence analysis with AI classification"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.3",
    "numpy>=2.4.4",
    "scikit-learn>=1.8.0",
]
